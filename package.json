{
  "name": "ecommerce-app",
  "version": "1.0.0",
  "description": "Full-stack e-commerce website with product pages, cart, and admin panel",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\" \"npm run admin\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "admin": "cd admin-client && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install && cd ../admin-client && npm install"
  },
  "keywords": [
    "ecommerce",
    "react",
    "express"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "axios": "^1.15.2",
    "nodemailer": "^8.0.6",
    "stripe": "^22.1.0",
    "twilio": "^6.0.0"
  }
}
